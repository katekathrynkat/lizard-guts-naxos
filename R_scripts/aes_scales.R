##############################
#####     Scales     #####
##############################

# Vectors for graph aesthetics

##### Color scales #####

# By trap type
scol_trap <- c("red", "orange")
names(scol_trap) <- c("pitfall", "sticky")


# By sample type
scol_sample <- c("blue", "red", "orange")
names(scol_sample) <- c("diet", "pitfall", "sticky")

# By veg level (high vs. low)
scol_veg <- c("darkgreen", "brown")
names(scol_veg) <- c("high", "low")

# By land use level (high vs. low)
scol_use <- c("grey", "tan")
names(scol_use) <- c("high", "low")
